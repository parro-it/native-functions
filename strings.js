import { unthis } from "unthis";

export const replace = unthis(String.prototype.replace);
export const trim = unthis(String.prototype.trim);
export const toUpperCase = unthis(String.prototype.toUpperCase);
